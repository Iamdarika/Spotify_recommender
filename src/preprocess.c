#include <stdio.h>
#include <omp.h>

void preprocess_data() {
    // Placeholder for parallel preprocessing with OpenMP
    printf("Preprocessing Spotify dataset with %d threads...\n", omp_get_max_threads());
}
